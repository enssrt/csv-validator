# Состояние проекта (Project State) — CSV Validator Pipeline

Этот файл фиксирует текущий прогресс разработки, принятые архитектурные решения и статус выполнения задач.

---

##  Текущий статус проекта
* **Текущая версия**: v2 (Развитие пайплайна и валидация данных)
* **Последнее обновление**: Июнь 2026 г.
* **Общий статус тестов**: 30/30 passed (Успешно подтверждено артефактом прогона)

---

##  Статус этапов разработки

### Версия v1: Базовый конвейер (Пайплайн) — [ВЫПОЛНЕНО]
* Реализовано чтение CSV-файлов, нормализация заголовков и проверка на наличие обязательных колонок (`name`, `date`, `price`).
* Написан базовый генератор отчетов на диск.

### Версия v2: Валидация данных (Row-level) — [В ПРОЦЕССЕ]

#### Подэтап 1: Интеграция validators.py в основной пайплайн — [ВЫПОЛНЕНО И ЗАКРЫТО]
* **Результат**: Функции построчной валидации `validate_price()` и `validate_date()` успешно интегрированы в `process_single_file()`. Ошибки данных собираются в `data_errors`, при их наличии файл переводится в статус `ERROR`. Все изменения покрыты интеграционными тестами (`30 passed`).

#### Подэтап 2: Фиксация слоя v2 в документации — [ВЫПОЛНЕНО И ЗАКРЫТО]
* **Результат**: Файл `README.md` полностью синхронизирован с текущим поведением кодовой базы. Описана работа двухуровневой валидации и зафиксированы точные ограничения. Конфликты в тексте устранены. Оперативный файл состояния приведен к единому стандарту имени `project-state.md`.

---

## Планы на будущее (Бэклог v2)
* Развитие CLI (интерфейса командной строки) / Обработка динамических флагов.
* Расширение логики построчной валидации для остальных колонок таблицы.